{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/phac-nml/cditoxins/master/assets/schema_input.json",
    "title": "phac-nml/cditoxins pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "assembly": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.f(ast)?a(\\.gz)?$",
                "errorMessage": "Fasta assembly file must be provided, cannot contain spaces and must have the extension: '.fa', '.fasta', '.fa.gz' or '.fastq.gz'"
            }
        },
        "required": ["sample", "assembly"]
    }
}
